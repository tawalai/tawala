# Chengelog

## v0.1.2

* Define `PolicyEngine` and `Rule` classes
* Add `BartLargeTextRule` as first core rule for text analysis
* Add `custom_guard_rule.py` as example of how to use a user defined `Rule`
* Add `standard_guard_rule.py` as example of how to use a core `Rule`

## v0.1.1

* Refactoring import of `TawalaClient` from official package

## v0.1.0

* Setup `TawalaClient` class
* Define main repositories and types to communicate with Tawala platform